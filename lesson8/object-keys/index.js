/* eslint-disable strict */
/* eslint-disable no-unused-vars */

'use strict'

// input obj
// output

const getKeys = (obj) => {
  const arr = Object.keys(obj);
  
  arr.forEach(el => {
  console.log(el);
  });
  }